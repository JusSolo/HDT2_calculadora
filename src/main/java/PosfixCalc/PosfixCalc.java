public interface PosfixCalc {
    /**
     * Realiza el cálculo de una expresión en notación posfija y devuelve el resultado.
     *
     * @param problem La expresión en notación posfija a evaluar.
     * @return El resultado del cálculo.
     */
    public Integer calc(String problem);
}
